# name: Makie CI
# on:
#   pull_request:
#     paths-ignore:
#       - 'docs/**'
#       - '*.md'
#     branches:
#       - master
#       - breaking-release
#   push:
#     paths-ignore:
#       - 'docs/**'
#       - '*.md'
#     branches:
#       - master
#       - breaking-release
#     tags: '*'

# concurrency:
#   group: ${{ github.ref }}
#   cancel-in-progress: true

# jobs:
#   test:
#     name: Julia ${{ matrix.version }}
#     runs-on: ${{ matrix.os }}
#     strategy:
#       fail-fast: false
#       matrix:
#         version:
#           - '1.6'
#           - '1' # automatically expands to the latest stable 1.x release of Julia
#         os:
#           - ubuntu-latest
#         arch:
#           - x64
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v2
#       - uses: julia-actions/setup-julia@v1
#         with:
#           version: ${{ matrix.version }}
#           arch: ${{ matrix.arch }}
#       - uses: julia-actions/cache@v1
#       - name: Install Julia dependencies
#         shell: julia --project=monorepo {0}
#         run: |
#           using Pkg;
#           # dev mono repo versions
#           pkg"dev . ./MakieCore"
#           Pkg.test("Makie"; coverage=true)

#       - uses: julia-actions/julia-processcoverage@v1
#       - uses: codecov/codecov-action@v1
#         with:
#           file: lcov.info
# 

name: Makie CI
on:
  push:
    branches:
      - main
    tags: '*'
  pull_request:
concurrency:
  # Skip intermediate builds: always.
  # Cancel intermediate builds: only if it is a pull request build.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}
jobs:
  test:
    name: Julia ${{ matrix.version }} - ${{ matrix.os }} - ${{ matrix.arch }} - ${{ github.event_name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        version:
          - '1.7'
          - 'nightly'
        os:
          - ubuntu-latest
        arch:
          - x64
    steps:
      - uses: actions/checkout@v2
      - uses: julia-actions/setup-julia@v1
        with:
          version: ${{ matrix.version }}
          arch: ${{ matrix.arch }}
      - uses: julia-actions/cache@v1
      - uses: julia-actions/julia-buildpkg@v1
      - uses: julia-actions/julia-runtest@v1
      - uses: julia-actions/julia-processcoverage@v1
      - uses: codecov/codecov-action@v2
        with:
          files: lcov.info
  docs:
    name: Documentation
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v2
      - uses: julia-actions/setup-julia@v1
        with:
          version: '1'
      - uses: julia-actions/julia-buildpkg@v1
      - uses: julia-actions/julia-docdeploy@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: |
          julia --project=docs -e '
            using Documenter: DocMeta, doctest
            using ImpedanceMeasSingleChannel
            DocMeta.setdocmeta!(ImpedanceMeasSingleChannel, :DocTestSetup, :(using ImpedanceMeasSingleChannel); recursive=true)
            doctest(ImpedanceMeasSingleChannel)'

